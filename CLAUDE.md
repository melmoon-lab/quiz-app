# CLAUDE.md — quiz-app

このファイルは Claude Code がこのリポジトリで作業する際のガイドラインです。

## プロジェクト概要

**プロジェクト名**: quiz-app  
**内容**: 一般常識クイズアプリ  
**技術スタック**: HTML / CSS / JavaScript（バニラ、フレームワーク不使用）

## ディレクトリ構成（予定）

```
quiz-app/
├── index.html        # エントリーポイント
├── style.css         # スタイルシート
├── script.js         # アプリケーションロジック
├── questions.js      # クイズ問題データ
└── CLAUDE.md
```

## コーディング規約

- インデントはスペース2文字
- 文字コードは UTF-8
- JavaScript は ES6+ 構文を使用（`const` / `let`、アロー関数など）
- グローバル変数は最小限に抑える
- DOM操作は `querySelector` / `querySelectorAll` を使用

## 業務ルール

- ファイルの削除・上書き前には必ず確認を求めること
- 問題データ（`questions.js`）を変更する際は、既存の問題数・フォーマットを維持すること
- 新機能追加時はまず実装方針を提案し、承認を得てから実装すること

## クイズアプリの仕様

- 問題は選択肢形式（4択）
- 正解・不正解のフィードバックを即時表示
- 全問終了後にスコアを表示
- 問題はランダム順で出題する

## 注意事項

- 外部ライブラリ・フレームワークは使用しない（バニラJS のみ）
- `package.json` は不要（Node.js 環境に依存しない）
- ブラウザで直接 `index.html` を開けば動作する構成にすること

## GitHubリポジトリ

https://github.com/melmoon-lab/quiz-app
